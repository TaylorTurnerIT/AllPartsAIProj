# Core Dependencies for Diagram Analysis Pipeline

# Computer Vision and Image Processing
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# YOLO Object Detection (for symbol detection)
ultralytics>=8.0.0

# Additional dependencies that ultralytics may need
torch>=2.0.0
torchvision>=0.15.0

# Optional: For better visualization
matplotlib>=3.7.0

# Note: All other imports (json, pathlib, argparse, etc.) are part of Python's standard library
